name: grooveforge_plugin_api
description: >
  GrooveForge Plugin API (GFPA) — interfaces for building GrooveForge-compatible
  instrument, effect, and MIDI FX plugins that run on all platforms.
version: 1.0.0
homepage: https://github.com/grooveforge/grooveforge

environment:
  sdk: '>=3.0.0 <4.0.0'
